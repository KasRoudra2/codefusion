import React from "react";
import { NavBar, NotFound, Footer } from "../components";

const NotFoundPage = () => {
  return (
    <div>
      <NavBar />
      <NotFound />
      <Footer />
    </div>
  );
};

export default NotFoundPage;
